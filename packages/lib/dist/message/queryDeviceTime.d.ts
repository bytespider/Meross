import { Message, type MessageOptions } from './message';
export declare class QueryDeviceTimeMessage extends Message {
    constructor(options?: MessageOptions);
}
export default QueryDeviceTimeMessage;
