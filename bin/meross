#!/usr/bin/env node

const program = require('yargs/yargs')(process.argv.slice(2));

program
  .usage('Usage: $0 <command> [options]')
  .command(require('./meross-info'))
  .command(require('./meross-setup'))
  .demandCommand()
  .version()
  .help().argv;
